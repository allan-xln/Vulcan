# AI API

Minimal FastAPI service for health checks and future explainability endpoints backed by structured evidence only.

## Local run
```bash
source .venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

